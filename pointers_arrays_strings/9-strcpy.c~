#include "main.h"

char *_strcpy(char *dest, char *src)
{
  int i = 0;
  int len = 0;
  char *help = src;
  while (*help != '\0')
    {
      help++;
      len++;
    }
  help--;
  while (i <= len)
    {
      dest[i] = src[i];
      i++;
    }
  return (*dest);
}
