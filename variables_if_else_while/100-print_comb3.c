#include <stdio.h>

void main(void)
{
  int two_digit;
  for (two_digit = 01, two_digit <= 89, two_digit++)
    {
      putchar(i);
      if ( i !=89)
	{
	  putchar(44);
          putchar(32);
	}
    }
  return;
}
