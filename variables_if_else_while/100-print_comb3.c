#include <stdio.h>

void comb3(void)
{
  int two_digit;
  for (two_digit = 01, two_digit <= 89, two_digit++)
    {
      putchar(two_digit);
      if ( two_digit !=89)
	{
	  putchar(44);
          putchar(32);
	}
    }
  return;
}
