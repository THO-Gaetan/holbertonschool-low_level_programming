#include "main.h"

void print_diagonal(int n)
{
  while (n > 0)
    {
    _putchar(92);
    _putchar('\n');
    len = n + 1;
    while (len >= 0)
      {
	_putchar(' ');
	len--;
      }
    i--;
    }
  return;
}
  
